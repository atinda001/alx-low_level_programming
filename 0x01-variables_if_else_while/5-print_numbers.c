#include<stdio.h>
/**
 * main - program entry point
 *
 * Return: 0 is Success. Error code otherwise
 *
 */
int main(void)
{
	int a = 0;

	for (a = 0; a <= 9; a++)
	{
		printf(" %d \n", a);
	}
	return (0);
	putchar('\n');
}
