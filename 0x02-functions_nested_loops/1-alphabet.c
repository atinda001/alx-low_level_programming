#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0
 */
print_alphabet();
int main(void)
{
	print_alphabet();
	return (0);
}

	print_alphabet()
	{
	int a;

	for (a = 'a'; a <= 'z'; a++)
	{
		_putchar(a);
	}
	_putchar('\n');
	return (print_alphabet);
	}
