#include "main.h"

/**
 * jack_bauer -print_hours
 *
 * @x,a: hours
 * @y,b:minutes
 *Return: all minutes of day
 */

void jack_bauer(void)
{
	int x, a;
	int z, b;

	while (x = 0; x <= 2)
	{
		_putchar(x);
		x++;
	while (a = 0; a <= 3)
	{
		_putchar(a);
		a++;
	_putchar(':');

	while (z = 0; z <= 5)
	{
		_putchar(z);
		z++;
	}
	while (b = 0; b <= 9)
		{
			_putchar(b);
			b++;
		}
	}
	}
}
