#include "main.h"
/**
 * _isupper - chhecks upper case
 *@c: variable to be tested
 * Return 1 if upper else 0
 */

int _isupper(int c)
{
	if (c == _isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
