#include "main.h"
/**
 * string_toupper -converts lowercase to uppercase
 *
 * Return: uppercase string
 */
char *string_toupper(char *)
{
	char *ptr;

	printf("%p", toupper(char *ptr));
	return (0);
}
